// !CONFIG_UPDATE
// Feel free to delete this file and its parent folder (auth) if they are no longer necessary.

const AboutPage = () => {
  return <div>About Page</div>;
};

export default AboutPage;
